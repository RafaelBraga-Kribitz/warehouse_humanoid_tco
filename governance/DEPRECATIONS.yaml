# Deprecations — symbols slated for removal with sunset dates.
#
# Enforced by scripts/check_deprecations.py:
#   - any entry where today > sunset_date AND the leaf-name symbol still appears
#     anywhere under src/ => fail
#   - entries should be removed when the symbol is deleted
#
# A deprecation must reference an ADR explaining why.

deprecations: []
