# Session End — 2026-05-31

> Generated by `make session-end` at 2026-05-31T10:34:05.009315+00:00.

## Audit state at session end

- Findings: total=43 · open=1 · in_progress=0 · closed=42 · historical=1 · wont_fix=0
- Migrations in_progress: 0

## Findings touched this session

**Closed (16):**
- F-020 (PR #28) — Executive_Summary QMD + HTML aligned with CSV; `make exec-summary` target authored.
- F-021 (PR #24) — Throughput verification redirected from missing JSON key to CSV.
- F-022 (PR #21) — Wage band in QMD replaced with KV-sourced range.
- F-023 (PR #17) — Wrong KV (Handel → Spedition) across QMDs, config, source.
- F-024 (PR #30) — Context-aware regex replacing the dropped run-count invariant.
- F-025 (PR #31) — `unifolm_wbt` split into 5 per-subset entries with real HF SHAs; contract tightened to require 40-hex.
- F-026 (PR #26) — Dead `local_dir` entries in raw manifest realigned with on-disk reality.
- F-027 (PR #32) — Truncated-Normal service time → lognormal preserving mean+std exactly. ADR-0010 authored.
- F-028 (PR #33) — `predict_utilisation` + saturation gate (ρ ≥ 1 raises) added to simulation engine.
- F-029 (PR #22) — `"90% CI"` → `"90% output interval (p5–p95)"` in sensitivity printout.
- F-030 (PR #34) — `normalised_elasticity` + `compute_elasticity_ranking` added to OAT; sensitivity report carries ranking.
- F-031 (PR #38) — MC `humanoid_capex_eur` switched from Normal to lognormal (strictly positive).
- F-032 (PR #39) — Dead `max(transfer_factor, 0.05)` floor removed; replaced with explicit positive check.
- F-033 (PR #40) — `total_opex_5yr_eur` split into `_nominal` and `_pv`; downstream consumers updated.
- F-034 (PR #41) — `humanoid_throughput_multiplier` now propagates through TCO; S-future-2028 differentiates.
- F-035 (PR #42) — ADR-0009 v2 ships with honest derivation + tautology disclosure (v1 errors fixed).
- F-036 (PR #43) — `seed_all` + canonical `global_seed` wired into module_02; dead granular seeds removed.
- F-037 (PR #37) — README MC quantile digits templated against report JSON; relabel "CI" → "output interval".
- F-038 (PR #20) — Invalid 4-argument DAX `IF` → nested `IF`.
- F-039 (PR #19) — Stale Best NPV literal replaced with data-pointer placeholder.
- F-041 (PR #35) — New `pipeline-smoke` job runs module-02 + module-03 on every PR.
- F-042 (PR #36) — Docker smoke test no longer overrides `--entrypoint`; exercises the real CLI.

**Open (1):**
- F-040 — Empty notebook outputs. Closure requires populating `notebooks/01_data_profile_summary.ipynb` or removing the README claim. Blocked in this container: auto-mode classifier requires explicit user-named file authorization for notebook source modification, and neither `jupyter` nor `quarto` are installed to execute the notebook cells. User-selected "Skip F-040 for now" at session end.

## ADRs added or modified

- `governance/adrs/0010-lognormal-service-time.md` — Authored (F-027).
- `governance/adrs/0009-knapp-throughput-reference.md` — Rewritten as v2 (F-035), replacing the dropped v1 that had factual errors.

## Invariants installed (scripts and tests)

No new `tests/governance/test_*.py` were authored this session — every closed finding reused its pre-existing verification script. Three tests were **tightened** to prevent the failure modes Codex flagged on dropped PRs:
- `test_f024_run_count_docs.py` — context-aware regex per role, MC-line filter, negative-lookbehind on digit boundaries.
- `test_f025_raw_manifest_provenance.py` — now requires 40-char hex for `revision_sha` and ISO YYYY-MM-DD for date fields (not just non-null).
- `test_f030_tornado_normalisation.py` — symbol-pattern instead of substring; comments alone no longer satisfy.

The Adversary job (`check_closed_findings.py`) re-runs every closed verification_script on every PR — 42 of them now.

## Open questions for next session

1. **F-040** is the only open finding. To close it the next session needs either:
   - Explicit user authorization to modify `notebooks/01_data_profile_summary.ipynb` (then this session's hybrid approach — populate cell 2 with the shipped `derisk_inspection_report.json` content; cells 7,9,11,13 stay source-only or get try/except guards — would close it in ~10 minutes), or
   - A separate decision to remove the README "stakeholder transparency" claims and delete the notebook entirely (also closes via the test's `notebook absent → vacuously fixed` branch).
2. **Sensitivity multiplier propagation** — F-034 closed the headline-TCO gap but `analysis/sensitivity.py::SCENARIO_COMPOSITIONS` still treats S-future-2028 identically to S-hybrid-5050 in MC output. Worth a new finding if the differentiation matters in the sensitivity report.
3. **AgentProfile.seed dead field** — F-036 closure note flagged it; harmless but a clean-up candidate.
4. **Codex usage limits** — reached during this session; reviews after PR #38 returned "usage limit reached" notes. Doesn't affect merging but means automated review feedback paused.

## Recommended next-finding priority

If F-040 authorization is granted: close F-040 (only remaining open finding). Otherwise the queue is empty under the current `verification_script`-bearing findings.

After F-040 closes, the next session is **Steward**: run the audit briefing prompt (CLAUDE.md → AUDIT_PROCEDURE.md) to surface any new findings via fresh inspection. The "open questions" above are good first candidates.

---
*Read by `make session-start` of the next session.*
