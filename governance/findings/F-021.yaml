id: F-021
title: Executive_Summary_DE.qmd throughput numbers off by 2.7× vs module_02_simulation_report.json
category: stale_generated_outputs
kind: recurrence_invariant
status: closed
opened_at: 2026-05-30
closed_at: 2026-05-30
recurrence_count: 0
evidence: |
  Pre-closure: reports/Executive_Summary_DE.qmd:60 claimed throughput of
  "~959 Bestellungen/Schicht". The original test pointed at a
  `replication_summary` key in module_02_simulation_report.json that does not
  exist (the simulation report has only meta-keys: phase, total_runs,
  scenarios_tested, n_runs_per_scenario, simulation_runs_path). Truth lives
  in exports/tableau_public/tco_scenarios.csv where the per-scenario
  throughput_mean_orders_per_shift averages ~949 across all 5 scenarios.
  The QMD's "~959" claim is within 10 orders of truth (tolerance is ±50).
verification_script: tests/governance/test_f021_exec_summary_throughput.py
notes: |
  Closed by redirecting the verification script to its canonical truth source:
  exports/tableau_public/tco_scenarios.csv (the per-scenario throughput_mean
  column is the canonical aggregate the dashboards consume). The QMD claim
  itself was already within tolerance; the contract just needed to point at a
  real data file instead of a missing JSON key. Previously xfail (truth
  missing), now PASSED.
