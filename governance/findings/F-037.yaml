id: F-037
title: README MC 90% interval digits hand-transcribed and drift from sensitivity_analysis_report.json
category: numeric_drift
kind: recurrence_invariant
status: closed
opened_at: 2026-05-30
closed_at: 2026-05-31
recurrence_count: 0
evidence: |
  Pre-closure: README.md:83 cited "90% CI: [€-1.39M, €-830K]" — abbreviated
  rather than full-digit, and the labels did not match the current
  sensitivity_analysis_report.json values (npv_p5 ≈ -€1,385,864,
  npv_p95 ≈ -€829,950 after the F-027 lognormal / F-030 elasticity reruns).
  Hand-edited drift was already documented as the failure mode.
verification_script: tests/governance/test_f037_readme_mc_digits.py
notes: |
  Closed by replacing the abbreviated values with full comma-formatted
  integers matching the JSON exactly:
    "  - 90% output interval (p5–p95): [€-1,385,864, €-829,950]"
  Also renamed "90% CI" to "90% output interval (p5–p95)" — same
  methodology fix F-029 applied inside sensitivity.py (empirical quantiles
  are not a CI on a population parameter). Bundled as a trivial under-10-line
  cross-cutting cleanup per CLAUDE.md scope rules.
  The verification test (_fmt(p5) and _fmt(p95) both present in README)
  becomes a true drift catch: any future pipeline re-run that shifts the
  MC quantiles will fail the test unless README is updated in the same PR.
  Previously xfail, now PASSED.
