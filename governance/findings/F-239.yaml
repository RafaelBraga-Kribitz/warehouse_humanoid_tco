id: F-239
title: "uv.lock stale — analytics extra missing; CI unlocked install"
category: fragmented_standards
kind: recurrence_invariant
status: closed
opened_at: 2026-07-16
closed_at: 2026-07-16
recurrence_count: 0
evidence: >
  uv.lock lacked provides-extras analytics / dbt-duckdb while pyproject declared
  the analytics extra. Makefile setup synced only --extra dev. CI used unlocked
  uv pip install -e ".[dev,analytics]" instead of uv sync --frozen.
verification_script: tests/governance/test_f239_uv_lock_analytics.py
notes: |
  ACCEPTANCE: uv.lock includes analytics/dbt-duckdb; requirements.txt refreshed;
  Makefile setup uses --extra dev --extra analytics; pipeline.yml + ci.yml use
  uv sync --frozen --extra dev --extra analytics before dbt/tests.
