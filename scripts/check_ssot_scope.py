"""SSOT scope coverage check (Phase 1 stub).

Will verify .pre-commit-config.yaml ssot-check `files:` pattern covers every
SSOT_REGISTRY canonical path.
"""

from _governance_stub import main_stub

if __name__ == "__main__":
    main_stub("check_ssot_scope.py", finding_id="F-009")
